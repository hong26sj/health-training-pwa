{
  "name": "health-training-ios-wrapper",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "ios:prepare": "node scripts/prepare-web.mjs",
    "ios:add": "npm run ios:prepare && npx cap add ios",
    "ios:sync": "npm run ios:prepare && npx cap sync ios"
  },
  "dependencies": {
    "@capacitor/core": "^8.0.0",
    "@capacitor/ios": "^8.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.0.0"
  }
}
